- **快速启动**：执行 `quick-start.sh` 可通过 macOS launchd 自动注册并守护进程。
- **环境配置**：支持通过 `.env` 或环境变量（如 `EZ_COMFYUI_PORT`, `JWT_SECRET_KEY`）自定义运行时参数。
- **依赖安装**：需手动安装 `fastapi`, `uvicorn`, `aiofiles`, `pillow`, `python-jose`, `bcrypt`。