- 前端模块采用 IIFE 包裹并启用严格模式，通过 `window.CW` 暴露公共接口，私有函数以下划线开头。
- UI 更新遵循局部刷新原则，禁止全量重绘列表，必须通过 data 属性定位 DOM 并进行增量更新。
- 样式定义强制分离至 CSS 文件，禁止在 HTML 或 JS 中使用行内 style 属性，动态状态通过 class 切换实现。
- 图标系统统一使用 `CW.icon(name)` 渲染 SVG，严禁在代码中直接使用 emoji 或 unicode 字符作为图标。