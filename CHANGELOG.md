# 更新日志

## v4.6.24

v4.6.24 修正刷新时首屏短暂显示未渲染 HTML 的问题。

- 主样式表改为在 `index.html` 的 `<head>` 中直接加载，并且位于 `module_loader.js` 之前，避免等待 JS boot 后才插入 CSS。
- `module_loader.js` 保留 `cwStyleLink` 兜底检测，不会重复加载主样式。
- 为首屏 CSS 加载顺序增加契约测试，后续 CSS 拆分时仍需保持基础样式先于页面渲染。

## v4.6.23

v4.6.23 进一步修正共享弹窗背景 mask 入场时序。

- 登录、确认、工作流/设备/节点等共享弹窗打开时，overlay 与毛玻璃 mask 立即可见，不再跟随父层 opacity 淡入。
- 关闭时保留短淡出，窗口本体仍保留位移与淡入动画，避免出现“窗口先移入、背景后出现”的两阶段感。

## v4.6.22

v4.6.22 修正共享弹窗背景 mask 与窗口入场不同步的问题。

- 登录、确认、工作流/设备/节点等共享弹窗的 backdrop 伪层不再二次淡入，避免半透明毛玻璃背景比窗口晚出现。
- 保留窗口本体的位移与淡入动画，由父 overlay 统一控制背景 mask 的出现时序。

## v4.6.21

v4.6.21 修正移动端失败任务卡片的内部排版。

- 失败卡片的错误图标、状态文字和重试按钮改为居中纵向布局，避免窄卡片内错位。
- 删除按钮固定在失败卡片右上角，标签区域避让删除按钮。

## v4.6.20

v4.6.20 修正非登录状态公开历史卡片预览和筛选懒加载提示。

- `/api/images/{filename}` 与 `/api/thumbs/{filename}` 改为按历史记录可见性放行，匿名用户可读取公开且未隐藏/未删除/未待审的媒体。
- 历史卡片、视频封面和完成动画统一媒体 URL 编码，避免特殊路径导致预览异常。
- 移除筛选空结果自动加载上限触发的“继续查找”按钮，历史出图卡片不再呈现误导性的手动上限入口。

## v4.6.19

v4.6.19 清理旧 ComfyUI Prompt 辅助实例链路。

- `config/nodes.json` 移除 `Prompt:8191` 实例，只保留 A/B 生成实例。
- 图片反推、提示词优化、翻译继续走本地 LLM API；LLM vision 不可用时不再 fallback 到 ComfyUI Prompt 实例。
- 状态 API 和前端实例指示器移除 `prompt_aux` / `P` 特例，实例状态仅展示实际返回的生成实例。

## v4.6.18

v4.6.18 修正历史画廊删除卡片后首屏窗口补位和排序错乱的问题。

- 删除历史卡片后，会按当前 `historyItems` 顺序立即补齐可见窗口中缺失的下一张卡片。
- 修正首屏 9 张之后删除卡片时 DOM 窗口与数据窗口脱节导致的顺序错乱。

## v4.6.17

v4.6.17 修正失败任务点击重试后旧错误卡片滞留的问题。

- `/api/jobs/{id}/retry` 创建新任务后立即移除旧失败 job，并在响应中返回 `dismissed_job_id`。
- 前端 `retryJob()` 成功后会立刻删除本地旧错误卡并强制刷新画廊，不再等待下一轮轮询清理。
- 保留 `/dismiss` 对历史 `retrying` 记录的兼容处理，但新 retry 流程不再生成持久 `retrying` 卡片。

## v4.6.16

v4.6.16 修正出图完成前保存/校验阶段的任务卡片状态层错位。

- 保存结果、内容校验等无最终图片状态改为统一渲染到 `.job-status-overlay`，spinner、状态文字和计时器共享同一个居中锚点。
- 任务状态 overlay 避开底部信息栏并固定在图片区中心，避免完成动画包裹旧卡片内容时文字和动画层错位。
- 迟到的 `generating_at` 计时器会插入到 overlay 内，保持状态文字和计时器排列稳定。

## v4.6.15

v4.6.15 继续处理历史画廊/任务卡片 C-1 去重问题，将 `card_manager.js` 收缩为薄适配器。

- `history.js` 成为画廊卡片 HTML、稳定 DOM patch、视频预览遮罩、批次卡片、失败卡片保留和任务排序的唯一实现来源。
- `card_manager.js` 不再携带第二套 `_renderJobCard` / `_renderHistCard` / `_patchGalleryHTML` 等重复 renderer，仅保留 PollManager 需要的 `CardManager` 运行时接口，并委托到 `history.js` 导出的函数。
- 将零进度生成任务的 `progress-unknown` 活动条逻辑迁回 `history.js`，避免移除 CardManager 重复实现后丢失可见活动反馈。
- 更新前端合同测试，防止 `card_manager.js` 再次出现历史渲染、视频预览、懒加载和状态文案重复实现。

## v4.6.14

v4.6.14 开始处理历史画廊/任务卡片 C-1 去重问题，先退役 `app.js` 中的遗留任务数据源。

- `app.js` 不再创建独立 WebSocket，也不再启动旧版 `pollJobs()` / `_pollActiveJobs()` / `onJobUpdate()` 任务状态合并链路。
- PollManager 现在是任务更新、主动轮询、计时器刷新和 `window.CW.onJobUpdate` 的唯一来源，避免双 WebSocket 与双重渲染入口互相覆盖。
- `app.js` 仅保留任务卡片所需的时间格式化工具和初始化胶水，降低后续合并历史/卡片渲染模块的风险。
- 增加静态测试，防止旧版任务数据源重新出现在 `app.js`。

## v4.6.13

v4.6.13 修正历史画廊与任务卡片生命周期中的前端稳定性问题。

- 历史筛选无匹配时限制自动级联加载，超过上限后改为用户手动继续查找。
- 历史初始加载、懒加载和加载失败增加可见反馈，失败时通过 toast 提示。
- 删除最后一张历史卡片时立即显示空状态，不再等待原子删除保护窗口结束。
- 任务计时器更新改为 `requestAnimationFrame` 节流，并清理已见 terminal job 记录。
- app 初始化增加防重入保护，避免重复绑定轮询和事件监听。

## v4.6.12

v4.6.12 修正取消任务的生命周期语义。

- 用户主动取消的任务会立即从本地任务列表和前端卡片中移除，不再作为 `cancelled` 卡片持久展示。
- 失败任务仍会保留，支持用户重新生成或手动删除失败记录。
- `/api/jobs/{id}/dismiss` 仅用于失败/重试中的记录，避免把主动取消混入失败处理流程。

## v4.6.11

v4.6.11 修复失败/取消任务在重启或刷新后丢失的问题。

- `jobs.json` 现在只过滤已完成任务，失败、取消和重试中的任务会持久化并在启动后恢复展示。
- 新增失败/取消/重试记录 dismiss 流程，取消任务保留为 `cancelled` 卡片，用户主动删除才会移除。
- 重试失败任务时旧卡进入 `retrying`，新任务成功后再自动清理旧失败记录。
- 启动恢复会跳过 error/cancelled/retrying，避免持久化失败卡被误恢复成运行任务；服务关闭前会保存一次最新任务状态。

## v4.6.10

v4.6.10 统一右上角 ComfyUI 容器指示器状态模型与颜色语义。

- 状态栏不再在“运行中 xx%”和 `A/B/P` 摘要之间切换；WebSocket/job update 也只更新 `A/B/P` 摘要。
- `A/B/P` 分段颜色调整为：关闭浅灰、待机绿色、运行随 GPU 压力动态显示绿色/橙色/红色。

## v4.6.9

v4.6.9 修正手机端 ComfyUI 容器指示器显示不完整的问题。

- 移动端状态栏右侧服务区改为内容自适应布局，状态摘要不再被 42px/70px 宽度截断，也不会强行铺满整列。
- 手机端隐藏固定 `ComfyUI` 标签，把空间优先留给 `A/B/P` 实例状态，保持 off/idle 非高亮、running/pending 分段高亮。

## v4.6.8

v4.6.8 修正出图队列状态展示和实例指示器口径。

- 排队卡片的长状态文字允许在卡片内换行，避免 `排队等待 B 当前任务完成...` 被截断。
- `/api/status` 与 `/api/comfyui/status` 不再把离线实例上的本地 queued/preparing 任务合并成实例远端队列，右上角 ComfyUI 实例指示器以真实容器 up/down 与远端队列为准。
- 保留已有 `prompt_id` 的运行任务在实例瞬时不可达时的恢复显示，避免长任务被临时健康检查误判为消失。
- 实例状态按稳定顺序显示为 A/B/P；A 未启动时明确显示 `A: off` 且保持非高亮，Prompt 在顶部摘要中缩写为 `P`。

## v4.6.7

v4.6.7 弃用早期出图前 LLM 容器让路策略，保留提示词优化/图片反推使用的共享 LLM API。

- 生成、重试和恢复排队不再检查旧 `qwen36-vllm` Docker 容器状态，也不再根据该状态停启 LLM 容器。
- `vllm_running()` / `start_vllm()` / `stop_vllm()` 默认变为 legacy no-op，仅在显式设置 `EZ_ENABLE_LEGACY_VLLM_MANAGEMENT=1` 时保留兼容行为。
- `/api/vllm/{action}` 默认返回弃用状态，避免手动误触旧容器启停策略影响 Qwen3.6 提示词优化服务。

## v4.6.6

v4.6.6 补齐安全审查中遗漏的 workflow 上传入口限额。

- `/api/workflows/meta/thumbnail` 改为分块读取并使用图片上传大小上限，避免缩略图上传无上限进入内存。
- `/api/workflows/{name}/upload-version` 保留 1MB workflow 版本上限，但改为统一分块读取路径，和其他上传端点保持一致。

## v4.6.5

v4.6.5 先处理出图链路 review 中会直接影响稳定性的部分。

- 修复 WebSocket 断线后 HTTP fallback 重新使用完整超时时间的问题，现在按整次追踪已消耗时间扣减剩余额度，避免串行队列被额外阻塞。
- 修复 `WSTracker.track()` / `resume()` 重置运行态时清掉开始时间的问题，确保 elapsed 和剩余超时基于真实追踪起点。
- 将 JobRunner 的提交无响应自动纠错从递归 `run()` 改为循环重试，释放实例后直接进入下一次尝试，并避免重试间重复恢复 vLLM。
- 确认当前主应用已把活跃任务判断注入 instance manager，`generating` 等活跃任务会阻止生命周期误杀；vLLM 队列级批量停启、连续任务热路径和 A/B round-robin 归入后续吞吐优化批次。

## v4.6.4

v4.6.4 继续收紧 cookie 会话后的写请求安全和资源入口。

- 为 cookie 会话的写请求增加 CSRF cookie/header 校验，登录和注册作为会话建立入口豁免。
- 登录和注册增加内存级 IP+用户名限流，降低暴力尝试风险。
- 图片、视频和工作流上传改为分块读取并设置默认大小上限，避免超大文件直接进入内存。

## v4.6.3

v4.6.3 收口浏览器端 token 暴露路径。

- 前端认证切换为 httpOnly cookie 会话，不再从 localStorage 读取或写入 `v4_token`。
- WebSocket 连接不再把 token 拼到 URL 查询参数，后端改为从握手 cookie 识别用户。
- 普通 API fetch 显式携带 cookie，并清理 workflow fallback 中的 localStorage token 读取路径。

## v4.6.2

v4.6.2 补齐 prompt 长度限制在 LLM prompt optimizer 入口的遗漏。

- `run_llm_prompt_optimizer()` 现在会在清洗 prompt 前先执行 10000 字符上限校验，封闭 S-9 的剩余绕过路径。

## v4.6.1

v4.6.1 聚焦 v4.6.0 安全审查后的高风险修复。

- 移除硬编码 JWT 默认密钥，未配置环境变量时改为生成每安装独立的本地密钥。
- 停止启动时自动创建 `admin/admin`，并要求管理员新建用户必须提供至少 6 位密码。
- 为 GPU 进程、工作流目录和媒体读取等敏感端点补充认证/管理员校验，并为登录补充 httpOnly cookie 兼容。
- 修复前端图库渲染中的错误消息和媒体 URL 属性转义问题，降低 DOM XSS 风险。
- 默认脱敏 LLM API key，并限制 prompt 入口最大 10000 字符。
- 收紧 JobRunner 取消流程，避免取消与运行 finally 双重释放实例信号量。

## v4.6.0

v4.6.0 聚焦 LTX2.3 视频工作流整理、10Eros 导演版实验链路和视频查看/编辑体验。

### 视频工作流

- 将 LTX2.3 Sulphur 拆分为快速版 fp8 与全量版 bf16，并保持 10Eros 使用 fp8mixed checkpoint。
- 新增 `i2v_ltx23_10eros_director` 工作流，接入 WhatDreamsCost 的 LTX Director / Director Guide 节点，用于多参考图分镜编排。
- 导演版工作流将主视频模型、音频 VAE 与 AV 文本编码器维持在同一套 10Eros checkpoint 上，减少底模不一致风险。

### 前端体验

- 快速出图界面为导演版 workflow 增加“导演模式”按钮，可展开浮动导演编辑区。
- 导演编辑区支持上传多张参考图、从历史图片拖入参考、设置每段起始帧/长度/强度/提示词，并在提交前写入 `timeline_data`。
- 视频查看模式增加逐帧预览、封面设置与导出入口，封面设置后走安全审查。

## v4.4.5

v4.4.5 聚焦图片反推颗粒度、专家团复核和 Z-Image LoRA 工作流，让复刻提示词更细、更可控，也让新增 LoRA 能直接进入生成流程。

### 图片反推

- 图片反推拆分为标准反推、专家反推和专家团反推，并让专家团在一次视觉读取中完成分工、复核和统一 JSON 输出。
- 专家反推强化自适应维度拆解，优先按基本概述、前景、主体、背景和整体画面组织，减少重复嵌套和模板字段。
- 增强颜色、发型发色、染发信息、发饰、服装材质、暴露内容和可见文字处理，颜色尽量绑定对象并输出近似 HEX。
- 新增镜头旋转倾斜、画面 roll、人物站位角度和主要关节点角度要求，细化头颈、肩肘腕、手掌、髋膝踝、脚尖与支撑关系。

### 模型与工作流

- 接入 DGX 上的 Qwen GGUF LLM 接口作为反推与提示词能力来源，并清理不需要的本地/远端模型服务。
- 新增 `z-image_loras` 工作流副本，接入 Z-Image 相关 LoRA，支持在工作流配置中调整 LoRA 强度。

## v4.4.3

v4.4.3 聚焦图片反推的专家模式、结构化提示词和本地 LLM 接入，让反推结果更适合直接用于复刻、边界验证和快速出图。

### 图片反推

- 标准反推和专家反推分流，专家模式按构图、颜色、氛围、肢体、表情妆造、性内容、服装、材质和摄影参数分别观察后再合并。
- 专家结果强制输出结构化 JSON，将正向画面描述和负面提示词分离，避免“没看到的内容”进入正向提示词。
- 强化服装、袜子、内衣、材质、色值、色温、光圈、焦段和肌理颗粒度描述，专家模式定位为更接近 1:1 复刻的高精度反推。
- 修正摄影参数表达，不再输出“推断/可能”等分析词，并把光圈大小关系写成可供生图模型参考的明确参数。

### LLM 与模型

- 增加本地/外部 LLM API 管理能力，支持多套接口配置、测试和快速切换。
- 接入本地 GGUF 视觉语言模型方案，并将 Qwen3.5 9B uncensored Q4 视觉模型作为备用模型放入项目模型目录。
- 优化大图反推超时与专家反推兜底 JSON 生成逻辑，减少模型输出缺字段时前端无内容的问题。

### 管理与出图

- 系统设置按内容板块分类并支持 tab 切换，LLM 接口测试按钮补充图标和结果显示优化。
- 通知管理支持二次编辑和删除。
- 出图调度限制为单一生成进程，避免两个实例同时抢占生成资源。

### 前端体验

- 图片反推上传窗口直接提供“标准反推 / 专家反推”两个入口，移除结果窗内二次专家反推按钮。
- 专家结果窗移动端按钮间距、图标和等待状态优化，提示词内容区域限制高度并支持滚动。
- 专家提示词操作拆分为复刻完整、复刻正向、复刻负面：点击后直接回填快速出图输入框，完整/负面会优先写入独立负面提示词框。

## v4.4.2

v4.4.2 聚焦内容保护、LLM 提示词链路、视频任务恢复与历史列表性能，让图片/视频生成后的审查、反推和展示行为更可控。

### 内容保护

- 引入生成后图片校验流程，图片完成保存后先保持保护态，验证为 safe 后再清晰显示。
- 将 NSFW 文本风险、裸露信号和中英文敏感提示词纳入后台验证，并支持在系统设置中调整开关、阈值和提示词规则。
- 对历史库已有图片补充保护状态写入和兜底重检，避免旧图在规则切换后失去保护。
- 视频卡片预览也纳入审查流程，保护状态会同步到视频缩略图展示。

### LLM 与提示词

- 提示词优化、翻译和图片反推接入同一个本地 LLM 能力，减少对独立 ComfyUI Prompt 实例的依赖。
- 图片反推在视频工作流中会继续走视频脚本优化，并传入实际秒数、帧率和帧数来编排时间轴。
- 视频脚本优化会按完整片段范围组织动作，例如 `0-5.04秒` 或对应帧范围，而不是只生成静态画面描述。

### 视频与任务恢复

- 出图调度改为全局单任务串行执行，避免 A/B 两个实例同时进行生成。
- 提交任务时保存 `client_id`、`prompt_id` 和实例信息，重启后优先按实例重连 WS，必要时回退到 queue/history 兜底。
- 优化视频卡片遮罩、播放按钮、视频图标和任务卡片排序，减少完成卡片与追踪卡片错位。

### 历史与工作流

- 出图历史改为轻量摘要轮询结合懒加载，只拉取必要数据，减少刷新时的阻塞。
- 删除历史卡片后会同步刷新当前列表窗口，避免已删除卡片继续残留。
- 工作流卡片预览图改为优先使用当前用户最近生成结果，让卡片缩略图更贴近实际使用。

## v4.4.0

v4.4.0 聚焦图生图结果核对、远端实例恢复和运行状态表达，让生成后的检查、更换实例、卡住恢复和视频/图片混合任务都更稳。

### 图片对比

- 图生图历史在图片放大窗口左侧新增“图片对比”图标按钮。
- 点击按钮可在当前大图位置切换原图与新图，再次点击返回新图。
- 对比按钮与下载、分享、收藏保持同一套圆形工具按钮视觉，并使用左侧实心的 compare 图标。
- 仅对可找到原始输入图的图生图历史显示，普通文生图、视频和生成中任务不显示。

### 运行恢复

- 增强远端实例停止后的任务收敛，实例被停止时会结束对应残留任务并推动队列继续调度。
- 冷启动等待超时前会先强制重启一次实例，减少旧 ComfyUI 进程占住端口导致的启动假死。
- 新增 GPU 无活动监测，结合进度、节点、显存和 GPU 利用率判断长时间卡住的任务，并按限制重试。
- 实例健康检查会避开仍有活跃任务的实例，减少运行中误判和不必要重启。

### 状态与任务卡片

- 任务状态文案从“图片”改为更中性的“内容/结果”，视频、图片和混合输出使用同一套表达。
- 前端历史卡片和 job 卡片会归一化旧状态消息，减少“视频任务显示拉取图片”的错位感。

### 工作流

- 新增 LTX2.3 10Eros 视频工作流，并登记到工作流元数据中。
- Qwen Edit v2511 暴露更多高级参数，方便在工作流编辑器里调整。

## v4.3.0

v4.3.0 是从 v4.2.x 升级到正式可用版本的一次体验整理，重点放在登录持久化、通知发布、首页入口、工作流扩展，以及历史画廊的载入和刷新稳定性。

### 登录与账号体验

- 登录状态改为更长时间保留，正常情况下至少可维持一个月，减少频繁重新登录。
- 网站标题旁新增当前版本号，方便确认正在使用的版本。
- 账号相关入口继续保持在主界面内，不影响原来的工作流和出图操作。

### 网站通知

- 管理员可在网站内发送通知，只需要填写标题和内容。
- 每条通知都会自动保存发送记录，方便后续查看已经发布过的内容。
- 普通用户进入界面后会以浮窗看到通知。
- 多条通知可以点击“下一条”依次查看。
- 点击关闭只关闭本次浮窗，下次进入仍会显示。
- 点击“不再通知”后，旧通知不再弹出；当管理员发布新通知后，会重新提醒用户。

### 首页入口

- 新增登录前首页，用更简洁的方式介绍系统能力。
- 首页包含大标题、系统说明、工作流/作品等数据块、随机作品展示，以及功能介绍区域。
- 视觉语言保持当前系统配色，同时减少复杂装饰，让进入系统前的第一屏更清晰。
- 点击登录后进入原来的主工作界面，原有生成、历史、工作流管理流程保持不变。

### 工作流与模型

- 新增和整理 Flux.2 Dev / Flux.2 Klein 的文生图、图生图工作流。
- 新增 Qwen Rapid 相关工作流，包括轻量化和 SeedVR2 放大组合。
- 新增 SeedVR2 视频放大工作流，支持 2K / 4K 场景。
- 新增 LTX / Sulphur 视频生成相关工作流。
- 工作流元数据、缩略图、分类和前端展示进一步同步，减少“文件已存在但界面不可见”的情况。

### 生成与历史画廊

- 优化出图历史的懒加载和卡片稳定性，减少首次进入时一次性加载过多内容。
- 生成中的任务卡片改为局部更新，状态变化时不再轻易重绘整块历史区域。
- 管理员视角下的任务轮询改为先比对差异；没有真实变化时只同步数据，不刷新整个历史画廊。
- 修复 admin 页面中出图历史大约每 3 秒整体闪烁一次的问题。
- 优化启动阶段的工作流和历史加载顺序，减少重复请求，让页面载入更快。
- 修复页面初始化时重复触发历史/工作流加载的问题。

### 设备管理

- 优化设备管理在桌面大分辨率下的卡片宽度和布局。
- 保留手机端舒适的单列展示，同时让桌面端设备信息不再被压缩得过小。

### 图像保护与安全展示

- 出图后先进入“图片校验中”状态，由本地轻量校验写回保护状态后再显示。
- 历史卡片使用后端返回的保护状态展示，减少前端重复判断造成的不一致。
- 敏感图片预览、普通用户可见范围、管理员操作能力继续保持后端权限约束。

### 提示词与辅助能力

- 提示词助手支持图片反推、提示词优化和中英切换等能力。
- JSON 提示词的中英文切换更稳定。
- 快速出图和节点编辑中，提示词类字段识别更完整，减少“高级编辑器里有、快速出图里没有”的情况。

### 运行与恢复

- 冷启动、实例选择、任务恢复和取消逻辑继续增强。
- 手动删除或取消的任务不会再被恢复逻辑重新拉回历史卡片。
- 生成计时更贴近真实出图阶段，减少排队、启动、提交阶段对耗时判断的干扰。
- 启动脚本的 restart 流程修复，网站服务重启更稳定。

## v4.2.x 小版本回顾

### v4.2.3

- 新增图片保护校验流程：出图完成后先显示校验状态，校验完成后再按保护结果展示。
- 后端统一返回图片保护状态，前端减少重复判断。
- 管理员与普通用户在历史图像上的可见范围和操作权限进一步对齐。

### v4.2.2

- 修复 Safari / WebKit 下登录、注册等弹窗遮罩抖动或失效的问题。
- 弹窗遮罩拆分为独立合成层，登录界面在桌面和移动端更稳定。

### v4.2.1

- 修复 JSON 提示词中英切换的稳定性问题。
- 修复 `quick-start.sh restart` 相关流程。
- 增强提示词助手、状态栏、历史画廊和工作流管理的多处细节。
- 优化快速出图、历史复用、上传图片、日志面板和实例状态显示。
